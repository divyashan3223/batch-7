const Button = () => {
  return <button>click</button>;
};

export const Card = () => {
  return (
    <section>
      <h1>this is card component</h1>
    </section>
  );
};

export default Button;
